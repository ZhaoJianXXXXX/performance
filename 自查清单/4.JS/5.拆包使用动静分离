优先级：高

操作步骤：

我们看一下 JS 打包的动静分离中动态和静态的划分：

动态(Active)：指经常需要修改迭代的模块，这部分上线频率最高，如入口逻辑、页面组件等。因为经常发生变化，所以包含这部分模块的产物文件在客户端的强缓存效果表现不理想，需要经常下载更新该文件。
静态(Static)：指很少会进行变动的模块。这部分比较典型的是一些三方库和页面的公共逻辑，一般我们在业务迭代过程中不会去修该它。如果产物文件中只包含这部分模块，那么该文件在客户端的强缓存效果表现会很好，只要缓存不过期，用户就不需要重新下载更新该文件。
由上分析，我们可以把公共库单独打包，同时把业务强相关的内容也单独打包。