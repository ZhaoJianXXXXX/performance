优先级：高

主次分割的目的在于先加载处理主要模块，后加载处理次要模块。

我们看一下 CSS 打包的主次分割中主要和次要的划分特点：

主要模块(Major)：可以特指首屏依赖的模块，也可以进一步细化为用户的主要关注区域依赖的模块。这部分内容是需要最高优先级处理的，需要尽可能早的下载完成，尽可能早的解析。我们需要保证主要模块的代码量尽可能少，首先加载，从而获得最优体验。
次要模块(Minor)：指非首屏区域的内容，也可以指首屏区域中无关紧要的部分。我们允许这部分内容出现的稍晚，允许一定程度的页面渲染闪烁。这样可以把更多的网络资源让位给更重要的主要模块，提升页面整体体验。